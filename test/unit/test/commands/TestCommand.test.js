//--------------------------------------------------------
//-- Tests - Unit - Test - Commands - Serve Command
//--------------------------------------------------------
'use strict';

const { given, when, then } = require('./TestCommand.gwt');


test('Can run test with Jest on all files', () => {

});

test('Can run test with Jest on all files when specified', () => {

});

test('Can run test with Jest on unit test files when specified as option', () => {

});

test('Can run test with Jest on feature test files when specified as option', () => {

});

test('Can run test with Jest on standards test files when specified as option', () => {

});

test('Can run test with Jest on end-to-end test files when specified as option', () => {

});

test('Can run test with Jest on unit test files when specified as flag', () => {

});

test('Can run test with Jest on feature test files when specified as flag', () => {

});

test('Can run test with Jest on standards test files when specified as flag', () => {

});

test('Can run test with Jest on end-to-end test files when specified as flag', () => {

});

test('Cannot run test with more than one repository', () => {

});
